import type { Config } from "tailwindcss";

const config: Config = {
  content: [""],
  theme: {
    extend: {
      colors: {
        primary: "#FF0000",
      },
    },
  },
  plugins: [],
};

export default config;
